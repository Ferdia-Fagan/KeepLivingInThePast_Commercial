import {BrowserStateManager} from "../BrowserStateManager";


describe("browser state manager", () => {
    
    describe("addWebpageBookmark", () => {
        it('should add webpage bookmark', function () {
            const browserStateManagerInstance = new BrowserStateManager()

            browserStateManagerInstance.addNewWindowOpened()
        });

    })
    
})
