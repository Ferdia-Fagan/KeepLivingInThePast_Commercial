describe("sadf", ()=> {
    it("fdsa", () => {
        const x: number[] = []
        const y: number[] = [1]

        const l = !x.length
        const m = !y.length

        expect(!x.length).toBe(false)
        expect(!x.length).toBe(true)
    })
})